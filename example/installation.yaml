# Soliplex SQL Example Installation
#
# This demonstrates soliplex_sql tool integration.
# Run with: soliplex-cli serve example/installation.yaml

id: "soliplex-sql-example"

meta:
  # Register SQL tool config classes
  tool_configs:
    - soliplex_sql.config.ListTablesConfig
    - soliplex_sql.config.GetSchemaConfig
    - soliplex_sql.config.DescribeTableConfig
    - soliplex_sql.config.QueryConfig
    - soliplex_sql.config.ExplainQueryConfig
    - soliplex_sql.config.SampleQueryConfig

secrets:
  - secret_name: "URL_SAFE_TOKEN_SECRET"
    sources:
      - kind: "random_chars"

environment:
  - name: "INSTALLATION_PATH"
    value: "file:."

# Disable OIDC authentication for demo
oidc_paths:
  -

# Rooms are loaded from ./rooms by default
room_paths:
  - "./rooms"

# Thread persistence (in-memory for demo)
thread_persistence_dburi:
  sync: "sqlite://"
  async: "sqlite+aiosqlite://"

# Room authorization (in-memory for demo)
room_authz_dburi:
  sync: "sqlite://"
  async: "sqlite+aiosqlite://"
